{
  "name": "cc-visicore-lookup-sync",
  "author": "Andrew Hendrix - CriblPacks@VisiCoreTech.com",
  "tags": {
    "product": [
      "edge",
      "search",
      "stream"
    ]
  },
  "cribl": {
    "type": "app",
    "createAppScriptVersion": "0.4.0"
  },
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "package": "npm run build && node scripts/package.mjs"
  },
  "dependencies": {
    "@capra/core": "^1.9.0",
    "@capra/dx-tokens-postcss-plugin": "^0.4.0",
    "@capra/icons": "^1.8.0",
    "@capra/theme": "^1.4.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "displayName": "Lookup Sync",
  "description": "Sync lookup files from Cribl Search knowledge (e.g. KQL '| export to lookup' results from Cribl Lake) into Stream worker groups and Edge fleets. Compares content, uploads only what changed, commits scoped to just the touched lookup files, and deploys on demand — all with explicit confirmation.",
  "license": "Apache-2.0",
  "minLogStreamVersion": "4.18.0"
}
